import { render, screen } from '@testing-library/react';
import App from './App';

describe("app", () => {
  // test('solar system component is rendered', () => {
  //   render(<App />);
  //   const solarSystemComponent = screen.getByTestId(/solar system/i);
  //   expect(solarSystemComponent).toBeInTheDocument();
  // });

})

